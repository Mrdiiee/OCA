name: Verify page wire
on:
  push:
    branches: [main]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          grep -q 'privateTripEvents' app/page.jsx
          grep -q 'from("events")' app/page.jsx
          echo verified
