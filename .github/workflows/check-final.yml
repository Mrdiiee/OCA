name: Check final
on:
  push:
    branches: [main]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -n 'privateTripEvents' app/page.jsx || true
