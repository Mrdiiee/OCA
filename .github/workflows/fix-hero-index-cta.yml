name: Fix Hero Oxygen Index CTA
on:
  push:
    paths:
      - .github/workflows/fix-hero-index-cta.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch homepage CTA image positioning
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('app/page.jsx')
          s=p.read_text()
          old='.hero-index-btn img{display:block;width:100%;height:100%;object-fit:contain;object-position:center;pointer-events:none;-webkit-user-drag:none;user-select:none}'
          new='.hero-index-btn img{display:block!important;position:static!important;inset:auto!important;width:100%!important;height:100%!important;max-width:100%!important;max-height:100%!important;object-fit:contain!important;object-position:center!important;opacity:1!important;filter:none!important;pointer-events:none;-webkit-user-drag:none;user-select:none}'
          if old not in s:
              raise SystemExit('hero-index image CSS target not found')
          s=s.replace(old,new,1)
          p.write_text(s)
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/page.jsx
          git commit -m 'fix: contain Oxygen Index logo inside hero CTA' || exit 0
          git push
