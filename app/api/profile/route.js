import { NextResponse } from 'next/server';
import { createClient as createSupabaseAdmin } from '@supabase/supabase-js';
import { createServerSupabaseClient } from '../../../lib/supabase-server';
async function context(){const supabase=await createServerSupabaseClient();const{data}=await supabase.auth.getUser();if(!data?.user)return{error:'Silakan login terlebih dahulu.',status:401};const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return{error:'Konfigurasi server belum lengkap.',status:500};return{admin:createSupabaseAdmin(url,key,{auth:{autoRefreshToken:false,persistSession:false}}),user:data.user};}
export async function GET(){const c=await context();if(c.error)return NextResponse.json({error:c.error},{status:c.status});const{data,error}=await c.admin.from('profiles').select('id,full_name,phone,address,city,postal_code').eq('id',c.user.id).maybeSingle();if(error)return NextResponse.json({error:'Profil gagal dimuat.'},{status:500});return NextResponse.json({profile:data||null});}
export async function PUT(request){const c=await context();if(c.error)return NextResponse.json({error:c.error},{status:c.status});try{const b=await request.json();const full_name=String(b.name||'').trim(),phone=String(b.phone||'').trim(),address=String(b.address||'').trim();if(!full_name||!phone||!address)return NextResponse.json({error:'Nama, nomor HP, dan alamat wajib diisi.'},{status:400});const{data,error}=await c.admin.from('profiles').upsert({id:c.user.id,full_name,phone,address,updated_at:new Date().toISOString()},{onConflict:'id'}).select('id,full_name,phone,address,city,postal_code').single();if(error)return NextResponse.json({error:'Profil gagal disimpan.'},{status:500});return NextResponse.json({profile:data});}catch{return NextResponse.json({error:'Permintaan tidak valid.'},{status:400});}}
