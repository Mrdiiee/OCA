"use client";
import { useEffect, useMemo, useState } from "react";
const fmt=n=>"Rp "+Number(n||0).toLocaleString("id-ID");
const fallbackImage="https://images.unsplash.com/photo-1551632811-561732d1e306?auto=format&fit=crop&w=1200&q=85";
export default function Produk(){
 const[products,setProducts]=useState([]);const[loading,setLoading]=useState(true);const[loadError,setLoadError]=useState("");const[cart,setCart]=useState([]);const[open,setOpen]=useState(false);const[error,setError]=useState("");
 useEffect(()=>{let active=true;(async()=>{try{const r=await fetch("/api/products",{cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d.error||"Produk gagal dimuat.");if(active)setProducts(d.products||[])}catch(e){if(active)setLoadError(e.message||"Produk gagal dimuat.")}finally{if(active)setLoading(false)}})();return()=>{active=false}},[]);
 function add(p){if(!p.stock){setError("Produk sedang habis.");setOpen(true);return}setCart(c=>{const x=c.find(i=>i.id===p.id);if(x){const qty=Math.min(x.qty+1,p.stock);return c.map(i=>i.id===p.id?{...i,qty}:i)}return[...c,{...p,qty:1}]});setOpen(true);setError("")}
 function changeQty(id,delta){setCart(c=>c.map(i=>{if(i.id!==id)return i;const stock=products.find(p=>p.id===id)?.stock??i.stock;return{...i,qty:Math.max(0,Math.min(i.qty+delta,stock))}}).filter(i=>i.qty>0))}
 async function checkout(items=cart){if(!items.length){setError("Keranjang masih kosong.");return}setError("");try{const r=await fetch("/api/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({items})});const d=await r.json();if(!r.ok){setError(d.error||"Checkout gagal.");return}if(window.snap&&d.token)window.snap.pay(d.token);else setError("Pembayaran belum siap. Silakan coba lagi.")}catch{setError("Tidak dapat terhubung ke server checkout.")}}
 function buy(p){if(!p.stock){setError("Produk sedang habis.");setOpen(true);return}const item={...p,qty:1};setCart([item]);setOpen(true);checkout([item])}
 const total=useMemo(()=>cart.reduce((s,i)=>s+i.price*i.qty,0),[cart]);
 return <main className="page"><style>{`body{margin:0;background:#0b0b0a;color:#f7f6f3;font-family:Arial,sans-serif}.page{min-height:100vh;padding:40px clamp(20px,6vw,90px)}a{color:#f7f6f3}.back{display:inline-block;margin-bottom:70px;text-decoration:none}.eyebrow{color:#e1261c;font-size:12px;letter-spacing:2px}.title{font-size:clamp(52px,9vw,110px);line-height:.9;margin:12px 0 60px}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:20px;max-width:1100px}.card{border:1px solid #2e2c28;padding:28px;background:#151412}.visual{height:300px;background:#111;overflow:hidden;border-bottom:1px solid #2e2c28;position:relative}.visual img{width:100%;height:100%;object-fit:cover;display:block}.stock{position:absolute;left:12px;top:12px;background:#e1261c;color:#fff;padding:7px 9px;font-size:10px;font-weight:bold}.name{font-size:32px;margin:24px 0 8px}.kind{color:#8c897f}.price{font-weight:bold;margin-top:25px}.description{color:#b7b3aa;line-height:1.6}.actions{display:flex;gap:8px;margin-top:20px}.btn{flex:1;padding:12px;border:1px solid #eee;background:#eee;color:#111;font-weight:bold;cursor:pointer}.secondary{background:transparent;color:#eee}.btn:hover:not(:disabled){background:#e1261c;border-color:#e1261c;color:#fff}.btn:disabled{opacity:.45;cursor:not-allowed}.drawer{position:fixed;right:0;top:0;height:100%;width:min(420px,100%);background:#0b0b0a;border-left:1px solid #2e2c28;padding:28px;box-sizing:border-box;z-index:20;display:flex;flex-direction:column}.drawer-head{display:flex;justify-content:space-between;align-items:center}.row{display:flex;justify-content:space-between;align-items:center;padding:14px 0;border-bottom:1px solid #2e2c28;gap:12px}.close{background:none;border:0;color:#aaa;cursor:pointer}.qty{display:flex;align-items:center;gap:8px}.qty button{width:28px;height:28px;background:transparent;border:1px solid #2e2c28;color:#fff;cursor:pointer}.qty button:disabled{opacity:.35;cursor:not-allowed}.cart-list{flex:1;overflow:auto;margin-top:15px}.checkout{width:100%;padding:14px;border:1px solid #eee;background:#eee;color:#111;font-weight:bold;cursor:pointer}.checkout:hover{background:#e1261c;border-color:#e1261c;color:#fff}.error{border:1px solid #7c4b47;color:#ff8178;padding:12px;font-size:12px;line-height:1.5;margin:12px 0}.status{color:#8c897f;margin:-35px 0 30px}.empty-products{color:#8c897f;padding:30px 0}@media(max-width:700px){.grid{grid-template-columns:1fr}.page{padding:24px}}`}</style>
 <a className="back" href="/">← OXYGEN GEAR</a><div className="eyebrow">01 / EQUIPMENT</div><h1 className="title">PRODUK.</h1>
 {loading?<p className="status">Memuat produk...</p>:loadError?<div className="error">{loadError}</div>:products.length===0?<p className="empty-products">Belum ada produk aktif.</p>:<div className="grid">{products.map(p=><article className="card" key={p.id}><div className="visual"><img src={p.image||fallbackImage} alt={p.name}/>{!p.stock&&<span className="stock">HABIS</span>}</div><div className="kind">{p.category||"Gear"} · {p.kind||"Outdoor"}</div><h2 className="name">{p.name}</h2><p className="description">{p.blurb}</p><div className="price">{fmt(p.price)}</div><div className="kind">Stok: {p.stock}</div><div className="actions"><button className="btn secondary" disabled={!p.stock} type="button" onClick={()=>add(p)}>{p.stock?"Tambah ke keranjang":"Stok habis"}</button><button className="btn" disabled={!p.stock} type="button" onClick={()=>buy(p)}>Checkout</button></div></article>)}</div>}
 {open&&<div className="drawer"><div className="drawer-head"><h2>Keranjang</h2><button className="close" type="button" onClick={()=>setOpen(false)}>Tutup</button></div>{cart.length===0?<p>Keranjang kosong.</p>:<div className="cart-list">{cart.map(i=>{const stock=products.find(p=>p.id===i.id)?.stock??i.stock;return <div className="row" key={i.id}><span>{i.name} × {i.qty}</span><span>{fmt(i.price*i.qty)}</span><div className="qty"><button type="button" onClick={()=>changeQty(i.id,-1)} aria-label="Kurangi">−</button><button type="button" disabled={i.qty>=stock} onClick={()=>changeQty(i.id,1)} aria-label="Tambah">+</button></div></div>})}</div>}<h3 style={{textAlign:"right"}}>Total {fmt(total)}</h3>{error&&<div className="error">{error}</div>}<button className="checkout" type="button" onClick={()=>checkout()}>Checkout Sekarang</button></div>}
 </main>
}