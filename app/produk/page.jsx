"use client";
import { useState } from "react";
const products=[{id:"keygen-v1",name:"Keygen V1",kind:"Keril 45L",price:550000,text:"Keril utama untuk pendakian multi-hari dengan konstruksi ringan dan tahan medan."},{id:"running-vest",name:"Running Vest",kind:"Hidrasi 5L",price:375000,text:"Vest ringan untuk trail running dan fastpacking jarak menengah."}];
const fmt=n=>"Rp "+Number(n).toLocaleString("id-ID");
export default function Produk(){const[cart,setCart]=useState([]);const[open,setOpen]=useState(false);const[error,setError]=useState("");function add(p){setCart(c=>{const x=c.find(i=>i.id===p.id);return x?c.map(i=>i.id===p.id?{...i,qty:i.qty+1}:i):[...c,{...p,qty:1}]});setOpen(true);setError("")}function changeQty(id,delta){setCart(c=>c.map(i=>i.id===id?{...i,qty:i.qty+delta}:i).filter(i=>i.qty>0))}async function checkout(items=cart){if(!items.length){setError("Keranjang masih kosong.");return}setError("");try{const r=await fetch("/api/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({items})});const d=await r.json();if(!r.ok){setError(d.error||"Checkout gagal.");return}if(window.snap&&d.token)window.snap.pay(d.token);else setError("Pembayaran belum siap. Silakan coba lagi.")}catch{setError("Tidak dapat terhubung ke server checkout.")}}function buy(p){const item={...p,qty:1};setCart([item]);setOpen(true);checkout([item])}const total=cart.reduce((s,i)=>s+i.price*i.qty,0);return <main className="page"><style>{`body{margin:0;background:#0b0b0a;color:#f7f6f3;font-family:Arial,sans-serif}.page{min-height:100vh;padding:40px clamp(20px,6vw,90px)}a{color:#f7f6f3}.back{display:inline-block;margin-bottom:70px;text-decoration:none}.eyebrow{color:#e1261c;font-size:12px;letter-spacing:2px}.title{font-size:clamp(52px,9vw,110px);line-height:.9;margin:12px 0 60px}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:20px;max-width:1100px}.card{border:1px solid #2e2c28;padding:28px;background:#151412}.visual{height:300px;background:#111;display:grid;place-items:center;font-size:70px;border-bottom:1px solid #2e2c28}.name{font-size:32px;margin:24px 0 8px}.kind{color:#8c897f}.price{font-weight:bold;margin-top:25px}.actions{display:flex;gap:8px;margin-top:20px}.btn{flex:1;padding:12px;border:1px solid #eee;background:#eee;color:#111;font-weight:bold;cursor:pointer}.secondary{background:transparent;color:#eee}.btn:hover{background:#e1261c;border-color:#e1261c;color:#fff}.drawer{position:fixed;right:0;top:0;height:100%;width:min(420px,100%);background:#0b0b0a;border-left:1px solid #2e2c28;padding:28px;box-sizing:border-box;z-index:20;display:flex;flex-direction:column}.drawer-head{display:flex;justify-content:space-between;align-items:center}.row{display:flex;justify-content:space-between;align-items:center;padding:14px 0;border-bottom:1px solid #2e2c28;gap:12px}.close{background:none;border:0;color:#aaa;cursor:pointer}.qty{display:flex;align-items:center;gap:8px}.qty button{width:28px;height:28px;background:transparent;border:1px solid #2e2c28;color:#fff;cursor:pointer}.cart-list{flex:1;overflow:auto;margin-top:15px}.checkout{width:100%;padding:14px;border:1px solid #eee;background:#eee;color:#111;font-weight:bold;cursor:pointer}.checkout:hover{background:#e1261c;border-color:#e1261c;color:#fff}.error{border:1px solid #7c4b47;color:#ff8178;padding:12px;font-size:12px;line-height:1.5;margin:12px 0}@media(max-width:700px){.grid{grid-template-columns:1fr}.page{padding:24px}}`}</style><a className="back" href="/">← OXYGEN GEAR</a><div className="eyebrow">01 / EQUIPMENT</div><h1 className="title">PRODUK.</h1><div className="grid">{products.map((p,i)=><article className="card" key={p.id}><div className="visual">{i===0?"▱":"◇"}</div><div className="kind">{p.kind}</div><h2 className="name">{p.name}</h2><p>{p.text}</p><div className="price">{fmt(p.price)}</div><div className="actions"><button className="btn secondary" type="button" onClick={()=>add(p)}>Tambah ke keranjang</button><button className="btn" type="button" onClick={()=>buy(p)}>Checkout</button></div></article>)}</div>{open&&<div className="drawer"><div className="drawer-head"><h2>Keranjang</h2><button className="close" type="button" onClick={()=>setOpen(false)}>Tutup</button></div>{cart.length===0?<p>Keranjang kosong.</p>:<div className="cart-list">{cart.map(i=><div className="row" key={i.id}><span>{i.name} × {i.qty}</span><span>{fmt(i.price*i.qty)}</span><div className="qty"><button type="button" onClick={()=>changeQty(i.id,-1)} aria-label="Kurangi">−</button><button type="button" onClick={()=>changeQty(i.id,1)} aria-label="Tambah">+</button></div></div>)}</div>}<h3 style={{textAlign:"right"}}>Total {fmt(total)}</h3>{error&&<div className="error">{error}</div>}<button className="checkout" type="button" onClick={()=>checkout()}>Checkout Sekarang</button></div>}</main>}